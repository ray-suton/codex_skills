interface:
  display_name: "Homework Helper"
  short_description: "Infer repo tasks, delegate cleanly, and produce submission-ready output."
  default_prompt: "Use $homework-helper to scan the current repository, infer the most likely assignment or project task from workspace evidence, coordinate the right agent roles, complete the work, validate it, and return the 9-section report with a polished final report."
